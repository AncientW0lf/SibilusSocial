namespace Sibilus.Database
{
    /// <summary>
    /// An enum that contains all available datatypes for SQLite.
    /// </summary>
    public enum DbDatatype
    {
        INT,
        REAL,
        TEXT,
        BLOB
    }
}
